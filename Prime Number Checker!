#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {

    int i=1, num,factor=0;
    scanf("%d",&num);
    if(num<=1){
        printf("Not Prime");
    }
    while(i<=num){
        if(num%i==0){
            factor++;}
            i++;}
        if(factor==2){
            printf("Prime");
        }
        else{
            printf("Not Prime");
        }
    return 0;
}
