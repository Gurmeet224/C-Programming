#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {

    int i,num,multiply;
    scanf("%d",&num);
    for(i=1;i<=10;i++){
        multiply=num*i;
        printf("%d x %d = %d\n",num,i,multiply);
    }
    return 0;
}
