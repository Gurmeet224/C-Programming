#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {

    int i=1,num;
    scanf("%d",&num);
    if(num==1){
        printf("%d",i);
    }
    else{
        while(i<=num){
        if(num%i==0){
            printf("%d ",i);}
            i+=1;
        }
    }
    return 0;
}
