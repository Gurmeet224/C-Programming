#include<stdio.h>
int main(){
    int x,y;
    printf("Enter two no: ");
    scanf("%d %d",&x,&y);
    (x<y) ? printf("%d is largest  than %d",y,x) : printf("%d is largest than %d",x,y);
    return 0;
}
